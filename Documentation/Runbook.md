The BarmBuzz Enterprise Active Directory deployment was initiated by aligning the underlying infrastructure with Windows Server 2022 compliance standards and establishing a dual-adapter network topology. The PowerShell environment was prepared by setting the execution policy to RemoteSigned and installing essential DSC modules, including ActiveDirectoryDsc and NetworkingDsc. Following this, the system identity was solidified by standardizing the hostname to BB-DC01 and mapping the internal interface to a static IP of 192.168.1.10, ensuring a stable foundation for the subsequent forest promotion. Once the identity was established, the AD DS role was installed to initialize the barmbuzz.local forest, with functional levels set to Server 2016 to maintain a balance of modern security and compatibility.

With the domain active, a structured Organizational Unit hierarchy was deployed, featuring a Tier0 administrative silo and regional operational units like the Bolton HQ. This structure was reinforced with accidental deletion protection across all objects to ensure long-term integrity. Identity management was then implemented using the AGDLP model, where global groups for baristas, managers, and IT helpdesk staff were provisioned and populated with core user accounts like Ava, Bob, and Charlie. This phase ensured that functional roles were accurately mapped to resource-specific access groups from the outset.

To harden the environment, four critical Group Policy Objects were engineered to enforce registry-level security, such as disabling legacy LM hashes, mandating SMB signing, and restricting USB storage on POS terminals. The deployment concluded with the delegation of administrative authority, granting the IT Helpdesk granular permissions to manage workstation joins in the Bolton OU under the principle of least privilege. Final validation was performed through a comprehensive DSC audit and visual confirmation in ADUC, confirming that the domain is fully operational, policy-compliant, and ready for production.

####

I have used Ai to summarise my ai chat conversations

###